import { ReplaceTextModel } from "../Models/ViewerTypes";
import { SearchResult } from "../Search";
import { TextSourceData } from "../Search/types";
import { CustomHighlight } from "./CustomHighlight";
import { ICustomHighlight } from "./types";
export declare class SearchReplacementHighlight extends CustomHighlight implements ICustomHighlight {
    pageIndex: number;
    replaceWith: string;
    replaceData: ReplaceTextModel;
    sourceData: TextSourceData[];
    viewer: any;
    constructor(searchResult: SearchResult, replaceWith: string, searcher: any, pageIndex: number);
    private _paintReplacedTerm;
}
